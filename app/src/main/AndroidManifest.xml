<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginactivity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoginActivity">
        <activity
            android:name=".ui.NavActivity"
            android:label="@string/title_activity_nav"
            android:theme="@style/Theme.LoginActivity.NoActionBar"></activity>
        <activity android:name=".ui.CadastroActivity" />
        <activity android:name=".ConfigVisualActivity"
            android:parentActivityName=".ui.NavActivity"/>
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>